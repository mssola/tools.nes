The goal for this binary is twofold:

1. It provides a way for me to check that the API from `vnf` is not broken.
2. It allows me to test very specific conditions, like certain memory values
   mid-execution and stuff like that (i.e. pretty much as any user from `vnf`
   would do).
